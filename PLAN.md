# תוכנית: Help Guide למשתמש

## סקירה כללית

יצירת מדריך למשתמש שמסביר את כל הפלואו של האפליקציה בצורה ברורה ונעימה, מבלי להעמיס על המשתמש.

---

## בחירת גישת UI/UX

### אפשרויות שנשקלו:

| אפשרות | יתרונות | חסרונות |
|--------|---------|---------|
| **Tab חמישי** | נגיש תמיד | מעמיס על הניווט הקיים |
| **Modal מרכזי** | לא מעמיס, ניתן לסגור | חוסם את העבודה |
| **Sidebar Help Button** | זמין תמיד, לא מעמיס | מקום מוגבל ב-sidebar |
| **Expandable Guide (מומלץ)** | גמיש, step-by-step, לא מעמיס | דורש עיצוב טוב |

### **הבחירה: Help Button + Modal עם Tabs פנימיים**

**למה?**
- כפתור "?" קטן ב-header/sidebar - לא מעמיס
- לחיצה פותחת modal גדול עם מדריך מלא
- בתוך ה-modal: tabs לכל נושא - המשתמש בוחר מה רלוונטי לו
- אפשרות "Quick Start" לסקירה מהירה
- אפשרות לסגור בכל רגע

---

## מבנה ה-Help Guide

### 🏠 1. Quick Start (סקירה מהירה)
**כותרת:** "איך זה עובד? ב-60 שניות"

```
1. יוצרים מושגים → 2. כותבים טקסט עם סימון → 3. מעבדים → 4. שולחים לטלגרם
```

תרשים ויזואלי פשוט של הפלואו עם חיצים

---

### 📚 2. ניהול מושגים (Glossary Tab)
**כותרת:** "יצירה ועריכת מושגים"

**נקודות מפתח:**
- **הוספת מושג:** כפתור "+ Add New Term" → שם + הגדרה
- **עריכה עשירה:** עורך Markdown עם תצוגה מקדימה
  - Bold, Italic, Links, Images
  - כותרות (H3, H4)
  - רשימות, ציטוטים, קוד
- **חיפוש:** שורת חיפוש מסננת לפי שם או הגדרה
- **עריכה/מחיקה:** כפתורים בכל כרטיס מושג

**טיפ:** כל מושג מקבל עמוד Telegraph משלו עם קישור ייחודי

---

### ✏️ 3. תחביר הסימון (Syntax)
**כותרת:** "איך לסמן מושגים בטקסט"

**תחבירים זמינים:**
| תחביר | דוגמה |
|--------|--------|
| `term<?>` (ברירת מחדל) | `ה-CPU<?> מעבד נתונים` |
| `[[term]]` | `ה-[[CPU]] מעבד נתונים` |
| `{{term}}` | `ה-{{CPU}} מעבד נתונים` |
| `<<term>>` | `ה-<<CPU>> מעבד נתונים` |
| **מותאם אישית** | הגדר prefix/suffix משלך |

**איפה משנים?** Settings Tab → Marking Syntax

---

### 🔄 4. עיבוד טקסט (Process Text)
**כותרת:** "הפיכת טקסט לקישורים"

**הפלואו:**
1. מדביקים טקסט עם מושגים מסומנים
2. בוחרים פורמט פלט:
   - **Telegram** - לשליחה לערוץ
   - **Markdown** - לקבצים/דוקומנטציה
   - **HTML** - לאתרים
3. לוחצים "Process Text"
4. מקבלים:
   - טקסט מעובד עם קישורים
   - סטטיסטיקות (נמצאו/חסרים)
   - רשימת המושגים שזוהו

**דוגמה:**
```
קלט:  "ה-{{CPU}} וה-{{RAM}} עובדים ביחד"
פלט:  "ה-CPU וה-RAM עובדים ביחד"
       + קישורים לעמודי Telegraph
```

---

### 📱 5. שליחה לטלגרם
**כותרת:** "חיבור וכתיבה לטלגרם"

**שלבי ההגדרה:**
1. **הוספת הבוט לערוץ:**
   - מוסיפים את `@TelegraphGlossaryBot` כמנהל
   - נותנים הרשאת כתיבה

2. **מציאת Chat ID:**
   - ערוץ ציבורי: `@שם_הערוץ`
   - ערוץ פרטי: מהכתובת ב-web.telegram.org
     - פורמט: `-1001234567890`

3. **הגדרה באפליקציה:**
   - Settings Tab → Telegram Bot Settings
   - מכניסים Chat ID
   - בודקים חיבור

4. **שליחה:**
   - Process Text Tab
   - אחרי עיבוד → כפתור "Send to Telegram"
   - ההודעה נשלחת עם קישורים לחיצים

**חשוב:** לסמן URL לשמירת ה-Chat ID!

---

### 💾 6. שמירת הגדרות
**כותרת:** "איך לא לאבד את ההגדרות"

**עיקרון מפתח:** כל ההגדרות שמורות ב-URL!

- Access Token לטלגרף
- Chat ID לטלגרם
- תחביר סימון

**מה לעשות:**
1. אחרי כל שינוי הגדרות - **לסמן את הכתובת (Bookmark)**
2. הכתובת היא ה"סיסמה" שלכם
3. שיתוף הכתובת = שיתוף גישה מלאה

**אזהרה:** איבוד הכתובת = איבוד גישה למילון!

---

### 🤖 7. שילוב AI (אופציונלי)
**כותרת:** "צ'אט עם AI לניהול המילון"

**יכולות:**
- שיחה טבעית עם AI
- בקשות כמו "הוסף מושג API"
- AI יכול ליצור/לעדכן/למחוק מושגים
- תמיכה ב-Claude, OpenAI, Gemini

**הגדרה:**
- AI Integration Tab → הזנת API Key
- התחלת שיחה

---

## עיצוב ה-Modal

### Header:
```
📖 מדריך למשתמש                    [X]
```

### Navigation (Pills/Tabs):
```
[Quick Start] [מושגים] [תחביר] [עיבוד] [טלגרם] [הגדרות] [AI]
```

### תוכן:
- כרטיסים עם אייקונים
- צבעים רכים (info boxes)
- דוגמאות עם highlighting
- תמונות/GIFs קטנים (אופציונלי)

### Footer:
```
[סגור] [פתח בעמוד חדש]
```

---

## שלבי מימוש

### שלב 1: יצירת קומפוננטת Help Guide
- קובץ חדש: `components/help_guide.py`
- פונקציה `render_help_guide()` שמחזירה HTML/Markdown

### שלב 2: יצירת ה-Modal
- שימוש ב-`st.dialog` (Streamlit 1.33+) או custom CSS
- Tabs פנימיים עם `st.tabs`

### שלב 3: הוספת כפתור Help
- ב-Sidebar או Header
- אייקון "?" או "📖"
- לחיצה פותחת את ה-Modal

### שלב 4: עיצוב וסגנון
- CSS מותאם לקריאות
- RTL support לעברית
- Responsive לנייד

### שלב 5: תרגום (אופציונלי)
- עברית כברירת מחדל
- אנגלית כאפשרות

---

## קבצים שיושפעו

| קובץ | שינוי |
|------|-------|
| `components/help_guide.py` | **חדש** - קומפוננטת המדריך |
| `app.py` | הוספת כפתור Help וקריאה ל-modal |
| `styles/` | CSS מותאם (אם נדרש) |

---

## דוגמת קוד

```python
# components/help_guide.py

import streamlit as st

def render_help_guide():
    """Render the help guide modal with tabs for each section."""

    tabs = st.tabs([
        "🚀 Quick Start",
        "📚 מושגים",
        "✏️ תחביר",
        "🔄 עיבוד",
        "📱 טלגרם",
        "💾 הגדרות",
        "🤖 AI"
    ])

    with tabs[0]:  # Quick Start
        render_quick_start()

    with tabs[1]:  # Glossary
        render_glossary_help()

    # ... etc

def render_quick_start():
    st.markdown("""
    ## 🚀 איך זה עובד? ב-60 שניות

    ```
    1. יוצרים מושגים  →  2. כותבים טקסט עם סימון  →  3. מעבדים  →  4. שולחים
    ```

    **הפלואו הבסיסי:**
    1. **צרו מושג** - לשונית "Glossary" → "+ Add New Term"
    2. **סמנו בטקסט** - כתבו `מושג<?>` בטקסט שלכם
    3. **עבדו** - לשונית "Process Text" → הדביקו → עבדו
    4. **שלחו** - כפתור "Send to Telegram"

    ✅ **זהו!** המושגים הופכים לקישורים לחיצים
    """)
```

---

## סיכום

מדריך זה יספק למשתמש:
- ✅ הבנה מהירה של הפלואו (Quick Start)
- ✅ הסברים מפורטים לכל תכונה
- ✅ דוגמאות מעשיות
- ✅ טיפים חשובים (שמירת URL!)
- ✅ גישה לא מעמיסה (modal עם tabs)
- ✅ עיצוב נעים וקריא
